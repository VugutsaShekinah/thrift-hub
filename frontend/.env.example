# Copy to .env.local for a custom dev setup, or set at build time in CI/Docker.
# Leave unset in normal local dev — Vite's proxy (vite.config.js) makes /api
# same-origin against http://127.0.0.1:8000 automatically.
VITE_API_BASE_URL=/api
